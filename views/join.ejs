<!DOCTYPE html>
<html lang="en">
<head> 
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Join FORM</title>
</head>
<body>
    <form method="post" action="/auth/join">
        <fieldset>
          <legend>사용자 등록(모두 작성해주세요)</legend>
          
          <div>아이디<input type="text" name="p_id" id="p_id" class="checkId" placeholder="6자 이상 숫자와 영문자"required>&nbsp;&nbsp;
            <span style="color:red; font-size:10px" id="noId"></span>
            <% if(errors.p_id) {%>
            <span style="color:red; font-size:10px"><%= errors.p_id %></span></div>
            <% } %>
          <div>비밀번호<input type="password" name="password" id="password" class="checkPass" placeholder="8-16자 숫자와 영문자" required>&nbsp;&nbsp;
            <span style="color:red; font-size:10px" id="noPass"></span>
            <% if(errors.password) {%>
            <span style="color:red; font-size:10px"><%= errors.password %></span></div>
            <% } %>
          <div>비밀번호 확인<input type="password" name="rePass" id="rePass" class="checkRePass" placeholder="위와 동일하게 입력" required>&nbsp;&nbsp;
            <span style="color:red; font-size:10px" id="noRePass"></span>
            <% if(errors.rePass) {%>
            <span style="color:red; font-size:10px"><%= errors.rePass %></span></div>
            <% } %>
          <div>이름<input type="text" name="name" id="name" placeholder="ex)홍길동" required/></div>
          <div>생년월일<input type="text" name="birth" id="birth" class="checkBirth" placeholder="ex)19910101" required/>
          <span style="color:red; font-size:10px" id="noBirth"></span></div>
          <div>전화번호<input type="text" name="ph_no" id="ph_no" class="checkPhone" placeholder="-없이 입력해주세요" required/>
          <span style="color:red; font-size:10px" id="noPhone"></span></div>
          <div>주소<input type="text" name="addr" id="addr" placeholder="ex)조선시 한양구 한강동" required/></div>
          <div>이멜<input type="email" name="email" id="email" class="checkEmail" required/>
            <span style="color:red; font-size:10px" id="noEmail"></span>
          <% if(errors.email){%>
            <span style="color:red; font-size:10px"><%= errors.email %></span></div>
            <%}%>
          <div><input id="gender" name="gender" type="radio" value="M" checked>남자
              <input id="gender" name="gender"type="radio" value="F">여자
            </div>
          <input type="submit" id="canSubmit" value="가입"/>
        </fieldset>
      </form>
      <a href="/"><button>메인으로</button></a>
      <script src="js/jquery-1.11.2.min.js"></script>
      <script src="js/jquery.main.js"></script>
      <script src="js/validate.js"></script>
</body>
</html>