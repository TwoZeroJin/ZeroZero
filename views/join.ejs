<!DOCTYPE html>
<html lang="en">
<head> 
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Join FORM</title>
</head>
<script src="https://unpkg.com/axios/dist/axios.min.js"></script>
<body>
    <form method="post" action="/auth/join">
        <fieldset>
          <legend>사용자 등록(모두 작성해주세요)</legend>
          <div>아이디<input type="text" name="p_id" id="p_id" placeholder="6자 이상 숫자와 영문자"required>&nbsp;&nbsp;
            <% if(message){%>
            <span style="color:red; font-size:10px"><%= message %></span></div>
            <%}%>
          <div>비밀번호<input type="password" name="password" id="password" placeholder="8-16자 숫자와 영문자" required>&nbsp;&nbsp;
            <% if(noPass){%>
            <span style="color:red; font-size:10px"><%= noPass %></span></div>
            <%}%>
          <div>비밀번호 확인<input type="password" name="rePass" id="rePass" placeholder="위와 동일하게 입력" required>&nbsp;&nbsp;
            <% if(rePass){%>
            <span style="color:red; font-size:10px"><%= rePass %></span></div>
            <%}%>
          <div>이름<input type="text" name="name" id="name" placeholder="ex)홍길동" required/></div>
          <div>생년월일<input type="text" name="birth" id="birth" placeholder="ex)19910101"required/></div>
          <div>전화번호<input type="text" name="ph_no" id="ph_no" required placeholder="-없이 입력해주세요"/></div>
          <div>주소<input type="text" name="addr" id="addr" required placeholder="ex)조선시 한양구 한강동"/></div>
          <div>이멜<input type="email" name="email" id="email" required /></div>
          <div><input id="gender" name="gender" type="radio" value="M" checked>남자
              <input id="gender" name="gender"type="radio" value="F">여자
            </div>
          <input type="submit" onclick="" value="가입"/>
        </fieldset>
      </form>
      <a href="/"><button>메인으로</button></a>
</body>
</html>